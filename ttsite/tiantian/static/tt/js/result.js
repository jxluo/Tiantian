/**
 * Javascript for result view.
 */

result = {};

/**
 * Start run script when body is loaded.
 */
startScript = function () {
  if (base.initSearchComponent) {
    base.initSearchComponent();
  }
};
